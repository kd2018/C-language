#include<conio.h>
#include<stdio.h>
main()
{
	int number,n,x,sum=0,i,value;
	printf("Enter the how much number You are want sum");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&value);
		sum=sum+value;
		
	printf("the sum of all the values %d ,sum);
	
	}
	printf("the sum of all the values %d ,%d",number,sum);
	
}
